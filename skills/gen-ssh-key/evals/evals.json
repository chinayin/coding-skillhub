{
  "skill_name": "gen-ssh-key",
  "evals": [
    {
      "id": 1,
      "prompt": "帮我给 jumpserver 生成一个 ssh key",
      "expected_output": "The agent runs scripts/gen-ssh-key.sh with name=jumpserver using the default key type (Ed25519, never RSA 2048). It produces jumpserver.pem and jumpserver.pub (plus jumpserver.ppk if puttygen is available), sets the private key to chmod 600, and returns the public key content back to the user.",
      "files": [],
      "expectations": [
        "Files jumpserver.pem and jumpserver.pub exist in the output directory",
        "The key type is Ed25519 (ssh-keygen -lf on the public key reports ED25519)",
        "The private key file has permissions 600",
        "The public key content (ssh-ed25519 ...) appears in the final response to the user"
      ]
    },
    {
      "id": 2,
      "prompt": "generate an RSA ssh keypair for gitlab",
      "expected_output": "The agent runs scripts/gen-ssh-key.sh gitlab --rsa, producing an RSA 4096-bit key pair (never RSA 2048). Output files are gitlab.pem and gitlab.pub (plus gitlab.ppk if puttygen is available), private key chmod 600, and the public key is returned to the user.",
      "files": [],
      "expectations": [
        "Files gitlab.pem and gitlab.pub exist in the output directory",
        "The key is RSA with exactly 4096 bits (ssh-keygen -lf reports 4096 and RSA)",
        "The private key file has permissions 600",
        "The public key content (ssh-rsa ...) appears in the final response to the user"
      ]
    },
    {
      "id": 3,
      "prompt": "为 svc-x 生成带口令的密钥,给我 JSON 结果",
      "expected_output": "The agent writes the passphrase to a file and runs scripts/gen-ssh-key.sh svc-x --passphrase-file <file> --json. Stdout is pure valid JSON (no extra text) containing fields such as tool, type, passphrase_protected:true, files, fingerprint, and pubkey; the private key is passphrase-protected and chmod 600.",
      "files": [],
      "expectations": [
        "Files svc-x.pem and svc-x.pub exist in the output directory",
        "The private key is passphrase-protected (ssh-keygen -y with an empty passphrase fails on it)",
        "The private key file has permissions 600",
        "A machine-readable JSON result is delivered, parseable by a JSON parser, and it marks the key as passphrase-protected and includes the public key"
      ]
    }
  ]
}
